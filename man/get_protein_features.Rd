% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/API_querys.R
\name{get_protein_features}
\alias{get_protein_features}
\title{Fetch protein features from UniProt}
\usage{
get_protein_features(
  input_id,
  map_to_genomic = FALSE,
  type_limit = NULL,
  desc_regex = NULL,
  full_data = FALSE
)
}
\arguments{
\item{input_id}{Character. One of: a gene symbol (\code{"POMC"}), Ensembl gene ID
(\code{"ENSG00000115138"}), Ensembl transcript ID (\code{"ENST00000395826"}), or a
UniProt accession (\code{"P01189"}).}

\item{map_to_genomic}{Logical. If \code{TRUE}, amino acid feature coordinates are
mapped to GRCh38 genomic positions via the Ensembl REST API using the ENSP
ID from the canonical transcript. Default \code{FALSE}.}

\item{type_limit}{Character vector or NULL. Restrict to specific feature
types (e.g. \code{c("Domain", "Region")}). Default \code{NULL}.}

\item{desc_regex}{Character or NULL. Regex to filter features by description.
Default \code{NULL}.}

\item{full_data}{Logical. If \code{TRUE}, returns a list with both the features
table and the full parsed UniProt JSON. Default \code{FALSE}.}
}
\value{
A \code{data.table} of features with columns \code{type}, \code{description},
\code{start}, \code{end} (and \code{genomic_start}, \code{genomic_end}, \code{strand} if
\code{map_to_genomic = TRUE}). If \code{full_data = TRUE}, a named list with
elements \code{Features} and \code{Protein_data}.
}
\description{
Retrieves annotated features (domains, active sites, etc.) for a
protein. The input can be a gene symbol, Ensembl gene/transcript ID, or a
UniProt accession - the function resolves all IDs automatically.
}
\examples{
\dontrun{
  # All inputs below resolve to the same protein:
  get_protein_features("POMC")
  get_protein_features("ENSG00000115138")
  get_protein_features("ENST00000395826")
  get_protein_features("P01189")

  # With genomic coordinate mapping:
  get_protein_features("POMC", map_to_genomic = TRUE)

  # Filter to peptides only:
  get_protein_features("POMC", type_limit = "Peptide", map_to_genomic = TRUE)
  
  # Filter to helical regions only
  get_protein_features("COL1A1", desc_regex = "helical", map_to_genomic = TRUE)
  
  # Get full data
  get_protein_features("POMC",full_data=T)
}

}
