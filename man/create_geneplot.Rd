% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Lolliplot.R
\name{create_geneplot}
\alias{create_geneplot}
\title{Create a gene lolliplot}
\usage{
create_geneplot(
  gene_name = NULL,
  markers = NULL,
  marker_class = "snp",
  domains = NULL,
  value_col = "P_BOLT_LMM_INF",
  pvalue = T,
  title_use = "Gene Lolliplot",
  subtitle_use = NULL,
  colour_title = NULL,
  shape_title = NULL,
  min_exon_width = NULL,
  intron_atten_cuttoff = 500,
  max_squashed_length = NULL,
  marker_gene_buffer = 0.4,
  exon_fill = "steelblue",
  x_axis_labels = "all",
  major_sig_thresh = -log10(5e-08),
  major_sig_colour = "blue",
  minor_sig_thresh = -log10(0.05),
  minor_sig_colour = "red",
  plot_sig_lines = TRUE
)
}
\arguments{
\item{gene_name}{Character. Gene symbol, ENSG, or ENST ID.}

\item{markers}{A \code{data.table} of variant or domain results, or \code{NULL}.
For SNP mode must contain columns: \code{SYMBOL}, \code{POS}, \code{BETA}, \code{AC}, and the
column named by \code{value_col}. For domain/burden mode must contain \code{SYMBOL},
\code{start}, \code{end}, \code{name}, \code{BETA}, and \code{value_col}.}

\item{marker_class}{Character. One of \code{"snp"}, \code{"burden"}, or \code{"domain"}.
Default \code{"snp"}.}

\item{domains}{A \code{data.table} with columns \code{start}, \code{end}, \code{domain} (and
optionally \code{colour}) for annotating protein domains. Default \code{NULL}.}

\item{value_col}{Character. Column name in \code{markers} to use as the y-axis
value. Default \code{"P_BOLT_LMM_INF"}.}

\item{pvalue}{Logical. If \code{TRUE}, values are treated as p-values and
transformed to signed -log10(p). Default \code{TRUE}.}

\item{title_use}{Character. Plot title. Default \code{"Gene Lolliplot"}.}

\item{subtitle_use}{Character or NULL. Plot subtitle.}

\item{colour_title}{Character. Legend title for colour mapping.}

\item{shape_title}{Character. Legned title for shape mapping.}

\item{min_exon_width}{Numeric or NULL. Minimum exon box half-height in data
units. Auto-calculated if \code{NULL}.}

\item{intron_atten_cuttoff}{Numeric. Introns longer than this (bp) are
compressed. Default \code{500}.}

\item{max_squashed_length}{Numeric or NULL. Maximum display width (in bp)
for compressed introns. Auto-calculated if \code{NULL}.}

\item{marker_gene_buffer}{Numeric. Gap between gene body and lollipop stems.
Default \code{0.4}.}

\item{exon_fill}{Character. Fill colour for CDS exon boxes. Default
\code{"steelblue"}.}

\item{x_axis_labels}{Character. One of \code{"all"}, \code{"minimal"}, or \code{"none"}.
Default \code{"all"}.}

\item{major_sig_thresh}{Numeric. Y-value for the major significance line
(default genome-wide: \code{-log10(5e-8)}).}

\item{major_sig_colour}{Character. Colour for the major significance line.
Default \code{"blue"}.}

\item{minor_sig_thresh}{Numeric. Y-value for the minor significance line
(default: \code{-log10(0.05)}).}

\item{minor_sig_colour}{Character. Colour for the minor significance line.
Default \code{"red"}.}

\item{plot_sig_lines}{Logical. Whether to draw significance threshold lines.
Default \code{TRUE}.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots variant-level (SNP) or domain-level association results as a lolliplot
overlaid on the canonical transcript structure of a gene, with optional
intron compression and protein domain annotations.
}
\examples{
\dontrun{
  # Basic gene structure only
  create_geneplot("POMC",intron_atten_cuttoff=NULL)
  
  # Compressed introns
  create_geneplot("POMC",intron_atten_cuttoff=500)

  # With SNP results
  set.seed(42)
  pomc_snps <- data.table(
    SYMBOL = "POMC",
    POS    = c(sample(25161081:25162000, 40),sample(25164500:25164772, 10)),
    BETA   = rnorm(50, mean = 0.5, sd = 0.3),
    P_BOLT_LMM_INF = c(
      runif(45, 0.01, 1),      # background variants
      runif(4,  1e-6, 1e-4),   # suggestive hits
      5e-9                      # one genome-wide significant hit
    ),
    AC = sample(c(1, 5, 20, 100, 500, 2000), 50, replace = TRUE)
  )
  create_geneplot("POMC", markers = pomc_snps, value_col = "P_BOLT_LMM_INF", 
  title_use = "POMC burden")
  # Add on different colours and shapes
  pomc_snps[,class:=sample(c("ptv","missense"), 50, replace = TRUE)]
  create_geneplot("POMC", markers = pomc_snps, value_col = "P_BOLT_LMM_INF", 
  title_use = "POMC burden")
  pomc_snps[,`:=`(
    colour=fifelse(class=="ptv","red","orange"),
    colour_key=class
  )]
  create_geneplot("POMC", markers = pomc_snps, value_col = "P_BOLT_LMM_INF", 
  title_use = "POMC burden",colour_title="Class")
  pomc_snps[,`:=`(
    shape=fifelse(P_BOLT_LMM_INF < 0.05,16,1),
    shape_key=fifelse(P_BOLT_LMM_INF < 0.05,"Sig","Insig")
  )]
  create_geneplot("POMC", markers = pomc_snps, value_col = "P_BOLT_LMM_INF", 
  title_use = "POMC burden",colour_title="Class",shape_title="Significance")

  # Overlaying protein domains on SNP results
  pomc_protein_domains <- get_protein_features("POMC", type_limit = "Peptide", 
  map_to_genomic = TRUE)[,
    .(domain=description,start=genomic_start,end=genomic_end)][!is.na(start)]
  ]
  create_geneplot("POMC", markers = pomc_snps, value_col = "P_BOLT_LMM_INF",
                  domains = pomc_protein_domains, 
                  title_use = "POMC SNPs + domains")
                  
  # With domain results
  set.seed(42)
  pomc_domains <- data.table(
    SYMBOL = "POMC",
    name   = c("Signal peptide", "NPP", "ACTH", "alpha-MSH", "beta-MSH"),
    start  = c(25161081, 25161200, 25161500, 25161500, 25163000),
    end    = c(25161150, 25161450, 25161800, 25161600, 25163500),
    BETA   = c(1, 1, -1, 1, -1),
    P_BOLT_LMM_INF = c(1e-9, 0.02, 1e-4, 0.5, 0.03)
  )
  create_geneplot("POMC", markers = pomc_domains, marker_class = "burden",
                  value_col = "P_BOLT_LMM_INF", 
                  title_use = "POMC domain burden")
  # With custom domain colours
  pomc_domains[, colour := c("red", "orange", "steelblue", "pink", "green")]
  create_geneplot("POMC", markers = pomc_domains, marker_class = "burden",
                  value_col = "P_BOLT_LMM_INF", 
                  title_use = "POMC domain burden")

}

}
