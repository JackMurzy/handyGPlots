% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/API_querys.R
\name{uniprot_lookup}
\alias{uniprot_lookup}
\title{Look up between UniProt accession and gene name}
\usage{
uniprot_lookup(input_id)
}
\arguments{
\item{input_id}{Character. Either an HGNC gene symbol (e.g. \code{"POMC"}) or a
UniProt accession (e.g. \code{"P01189"}).}
}
\value{
A character string. If \code{input_id} is a gene symbol, returns the
best-matching UniProt accession. If \code{input_id} is a UniProt accession,
returns the corresponding gene symbol.
}
\description{
Searches the UniProt REST API and resolves either a gene symbol to its
UniProt accession, or a UniProt accession to its gene symbol, depending
on the input provided.
}
\examples{
\dontrun{
  uniprot_lookup("POMC")    # returns "P01189"
  uniprot_lookup("P01189")  # returns "POMC"
}

}
